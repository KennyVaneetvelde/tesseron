---
"@tesseron/docs-mcp": patch
---

Docs: the `@tesseron/mcp` pin on the gateway page now follows releases through `sync-plugin-version`, and the C++ installation page fetches the `v0.1.0` tag instead of `main`.
