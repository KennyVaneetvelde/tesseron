{
  "name": "express-prompts",
  "version": "0.0.0",
  "private": true,
  "description": "Express backend exposing a prompt library (actions + resources) to Claude via @tesseron/server, plus a REST HTTP API over the same state. Sampling-heavy domain: Claude tests, refines, and generates prompt variants via ctx.sample.",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@tesseron/server": "workspace:*",
    "express": "^4.21.0",
    "zod": "^4.3.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.0",
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}
