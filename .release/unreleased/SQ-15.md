---
ref: SQ-15
title: Rust SDK part 1 and a Windows fix for the conformance runner
bump: minor
plugins:
  - tesseron
---

A Rust SDK lands under `sdks/rust/`: handshake, claiming, resume with token rotation, action invocation with input validation and cancellation, and resource reads. The conformance runner also stopped choking on a relative `--host` path on Windows.
