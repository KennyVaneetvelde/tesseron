---
ref: SQ-24
title: C++ SDK with a conformance host and a full conformance pass
bump: minor
plugins:
  - tesseron
---

A C++ SDK lands under `sdks/cpp/`: the whole host half of protocol 1.2.0 except host-minted claim codes, built with CMake and `FetchContent` at pinned hashes. Handlers are C++20 coroutines on Boost.Asio. It passes every conformance fixture that doesn't need a host-minted claim or a unix socket.
